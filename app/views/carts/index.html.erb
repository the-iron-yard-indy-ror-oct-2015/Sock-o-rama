
<div class="container-fluid">

  <% @items.each do |i| %>
  <div class="row item-row">
    <div class="col-xs-1">
      <%=image_tag(i.sock.img_url, class:"img-responsive center-block cart-images") %>
    </div>
    <div class="col-xs-2">
      <div class="col-xs-10 col-xs-offset-1">
        <p>Name: <%= i.sock.name %></p>
      </div>
      <div class="col-xs-10 col-xs-offset-1">
        <p>Material: <%=i.sock.material%></p>
      </div>
    </div>
    <div class="col-xs-2">
      <p>Price: <%=number_to_currency(i.sock.price)%></p>
    </div>
    <div class="col-xs-2">
      <p>Quantity: <%=i.quantity%></p>
    </div>
    <div class="col-xs-2">
      <p>Item Total: <%=number_to_currency(i.sock.price * i.quantity) %></p>
    </div>
  </div>
  <% end %>
  <div class="row total-row">
    <div class="col-xs-2 col-xs-offset-8">
      <p>Your Total: <%= number_to_currency(@cart.total) %> </p>
    </div>
    <div class="col-xs-2">
      <%= link_to "Checkout", "#", class:"btn btn-primary center-block" %>
    </div>
  </div>

</div>
